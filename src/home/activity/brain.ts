/**
 * SMART BRAIN — stub. Replaced by the real implementation (pose-driven
 * activity sensing + presence/routine fusion). Exists so app wiring compiles
 * while the brain is under construction; both entry points are safe no-ops.
 */
export function startBrain(): void {}
export function stopBrain(): void {}
